testdata.newPaste.code= \
  git config --global user.name "New Sheriff in Town"\
  \ngit reset $(git commit-tree HEAD^{tree} -m "Legacy code") \
  \ngit push origin master --force;
testdata.newPaste.syntaxHighLighting=Bash
testdata.newPaste.pasteExpiration=Never
testdata.newPaste.nameTitle=How to gain dominance among developers